# TODO — wethechurch

Open items for this repo. Move to CHANGELOG when done.

---

## Content
- [ ] More truth series pages to write — see `/truth/truth-index/` for full planned list
- [ ] More articles to write — see `/articles/`
- [ ] PWA manifest — `icons/icon.svg` exists but no manifest.json yet

## Code quality
- [ ] `icons/generate-icon.html` is a utility tool — move to `/scripts/` or delete if no longer needed
- [ ] Truth pages share nearly identical CSS — consider extracting shared rules into a `truth-shared.css`

## Infrastructure
- [ ] Confirm Google Apps Script URL in `scripts/wtc-apps-script.js` is live and receiving submissions
- [ ] Add `manifest.json` for PWA support (icon already exists at `/icons/icon.svg`)
